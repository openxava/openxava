Pending UI8 modernization (tmr)
-------------------------------

- Pantallazos:
    - Lista de un módulo (con filas, filtros y botonera).
    - Detalle con secciones y frames (un módulo con varias secciones y una referencia).
    - Una @ElementCollection con algunas filas.
    - Un diálogo abierto (p.ej. añadir a colección o un diálogo de acción).
    - El calendario (vista mensual con eventos) y el popup calendar de un campo fecha.
    - Menú izquierdo + panel de chat abierto a la vez.
    - Sign in.

- Suite.
- Prueba manuales.
- Borrar MDs.