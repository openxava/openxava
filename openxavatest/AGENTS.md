# Instructions for AI Agents

## Testing Preferences
The user prefers to run tests manually from their IDE. Do not attempt to run tests using maven (`mvn test`) 
or any other command-line tool automatically.

## Development Notes
- OpenXava automatically generates a module for each entity, so it's not necessary to add a module in application.xml
- Always run `mvn compile` to verify the code compiles before assuming it works
